import { Link } from "react-router-dom";

const PageNotFound = () => {
  return (
    <div>
      PageNotFound
      <Link to="/"> Return to Shop</Link>
    </div>
  );
};

export default PageNotFound;
